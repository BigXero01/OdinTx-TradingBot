{
  "buildCommand": "cd frontend && pnpm install && pnpm run build",
  "outputDirectory": "frontend/dist",
  "routes": [
    { "src": "^/(.*\\..+)$", "dest": "/$1" },
    { "src": "^/(.*)$", "dest": "/index.html" }
  ],
  "env": {
    "VITE_API_URL": "@odintx_api_url"
  }
}
